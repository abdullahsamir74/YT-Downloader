# YouTube Downloader Requirements
# Install with: pip install -r requirements.txt

# Main dependency for downloading YouTube videos
yt-dlp>=2025.12.8

# Note: The following packages are automatically installed as dependencies of yt-dlp:
# - certifi (SSL certificates)
# - charset-normalizer (character encoding detection)
# - idna (internationalized domain names)
# - requests (HTTP library)
# - urllib3 (HTTP client)

# Additional requirement (not Python):
# - ffmpeg must be installed separately for subtitle embedding and audio conversion
#   Install from: https://ffmpeg.org/download.html
